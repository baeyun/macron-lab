export default
`<h1>Focus on a window and get focal state</h1>
<p>
  Use the <code>focus()</code> method to get or set focus on window.
  If you need to focus on the window on initial startup, use <code>focusOnStartup()</code>
  on the uninitialized window.
</p>

<button id="focus">Focus on window</button>
`