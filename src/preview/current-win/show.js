export default
`<h2>Show a window</h2>

<p>Use <code>show()</code> to show the current window only after it's hidden.</p>

<p>Try clicking this button to hide then show a window after every 2 seconds.</p>

<button id="start-interval">Start interval</button>
<button id="stop-interval">Stop interval</button>
`