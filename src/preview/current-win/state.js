export default
`<h2>Change window's state</h2>

<p>Use <code>state()</code> to change the current window's state.</p>

<p>
  There are only 3 states available (lowercase):
  <ul>
    <li>Maximized</li>
    <li>Minimized</li>
    <li>Normal</li>
  </ul>
</p>

<p>Uncomment the other states in the editor to view the changes.</p>

<button id="state-window">Change window's state</button>
`