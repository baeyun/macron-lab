export default
`<h2>Remove file from system (sync)</h2>

<p>Use <code>unlink(filePath: String)</code> to delete a file, where <i>filePath</i> is the relative/absolute file path.</p>

<button id="unlink">Delete file</button>
`