export default
`<h2>Copy file content (sync)</h2>

<p>
  Use <code>copy_file(src: String, to: String)</code> to copy a file's contents,
  where <i>src</i> is the relative/absolute source file path and <i>to</i> is the final file path (to copy to).
</p>

<button id="copy_file">Read file</button>
`