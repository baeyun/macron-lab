export default
`<h2>Remove directory with contents</h2>

<p>Use <code>rmdir(dir: String)</code> to delete a directory with contents, where <i>dir</i> is the relative/absolute directory path.</p>

<button id="rmdir">Delete path</button>
`