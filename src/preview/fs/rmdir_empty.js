export default
`<h2>Remove empty directory</h2>

<p>Use <code>rmdir_empty(dir: String)</code> to remove an empty directory, where <i>dir</i> is the relative/absolute directory path.</p>

<button id="rmdir_empty">Delete path</button>
`