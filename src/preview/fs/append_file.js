export default
`<h2>Append content to file (async)</h2>

<p>Use <code>append_file(filePath: String, fileContents: String)</code> to append to a file's contents,
where <i>filePath</i> is the relative/absolute file path and <i>fileContents</i> represents the actual file contents.</p>

<input id="contents" placeholder="Enter cat's name"></input>
<button id="append_file">Append file</button>
`