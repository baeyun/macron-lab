export default
`<h2>Read directory contents</h2>

<p>
  Use <code>read_dir: Array (path: String)</code> to read a directory's contents,
  where <i>path</i> is the relative/absolute directory path. Returns a list of all
  files or sub-directories in it.
</p>

<button id="read_dir">Read directory</button>
`