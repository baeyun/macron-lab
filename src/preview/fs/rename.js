export default
`<h2>Rename file or directory</h2>

<p>
  Use <code>rename(src: String, to: String)</code> to rename a file or a directory
  from <i>src</i> to another value (<i>to</i>). Should be noted that if the files/directories
  are not in the working directory you will need the full path.
</p>

<button id="rename">Rename</button>
`